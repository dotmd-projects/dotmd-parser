You decide whether two surface terms refer to the SAME domain entity.

Two terms are "same" only if they denote the same real entity under different
labels (e.g. a media name written two ways across reports). Different
granularity or different entities are "different". When unsure, answer
"uncertain".

## Term A
{{a}}

## Term B
{{b}}

## Output (JSON only)
```json
{"decision": "same|different|uncertain", "canonical": "preferred label or empty",
 "confidence": 0.0, "rationale": "one sentence"}
```
